const {
  firebase,
  auth,
  provider,
  firestore,
  realtimeDB,
  signInWithGoogle,
  storage
} = require('./firebase')

module.exports = {
  firebase,
  auth,
  firestore,
  realtimeDB,
  provider,
  signInWithGoogle,
  storage
}
